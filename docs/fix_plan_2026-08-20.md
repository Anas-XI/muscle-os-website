# Fix Plan — 2026-08-20

> Generated from reviewing all commits made on Aug 19, 2026.

---

## Issues Found

### ?? Issue 1 — Broken `Bearer` template literal in Worker (CRITICAL)

**File:** `website/worker/src/index.js` — Line 1218

The `handleAiCoach` function has a broken Authorization header that will throw a JS parse error on every Worker cold start, taking down `/api/ai-coach`.

```js
// ? Current (broken)
'Authorization': Bearer       },

// ? Fix
'Authorization': `Bearer ${env.LLM_API_KEY}`,
```

---

### ?? Issue 2 — AI Coach uses relative URL (`/api/ai-coach`) on GitHub Pages (CRITICAL)

**File:** `website/tools/training_tool.html` — Lines 9116–9118

The production fallback uses a relative path. The tool is hosted on GitHub Pages — there is no Worker at that origin, so every AI Coach message silently 404s.

```js
// ? Current (broken on GitHub Pages)
const ENDPOINT = window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1'
                 ? 'http://127.0.0.1:8787/api/ai-coach'
                 : '/api/ai-coach';

// ? Fix
const ENDPOINT = (window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1')
                 ? 'http://127.0.0.1:8787/api/ai-coach'
                 : 'https://muscleos-access-control.muscleos.workers.dev/api/ai-coach';
```

---

### ?? Issue 3 — Stale Gemini error message in AI Coach catch block

**File:** `website/tools/training_tool.html` — Line 9157

After migrating from Gemini ? Groq, the error message still mentions `GEMINI_API_KEY`.

```js
// ? Current (stale)
botBubble.innerHTML = '<span ...>Error: Could not reach coach. Please make sure GEMINI_API_KEY is configured in the worker.</span>';

// ? Fix
botBubble.innerHTML = '<span ...>Error: Could not reach AI Coach. Please try again later.</span>';
```

---

### ?? Issue 4 — Testing paywall bypass left active in `access-control.js`

**File:** `website/assets/js/access-control.js` — Lines 209 & 214

The `chore(auth): unlock all tools for testing` commit hardcoded bypass overrides. The real implementation is **unreachable** — every user gets master access without any code or Google session.

```js
// ? Current (bypasses all auth — real code unreachable)
getAccess: function(productId) { return { active: true, plan: "master", expiry: "2099-12-31...", token: "admin_override" };
  return getStoredAccess(productId); // unreachable
},
checkAccess: function(productId) { return Promise.resolve({ active: true, plan: "master", ... });
  return revalidateAccess(productId)... // unreachable
},

// ? Fix (restore real implementations)
getAccess: function(productId) {
  return getStoredAccess(productId);
},
checkAccess: function(productId) {
  return revalidateAccess(productId).then(function(access) {
    return access || Promise.resolve(getStoredAccess(productId));
  });
},
```

> ?? **Confirm before fixing:** Are we done testing and ready to restore real auth?

---

### ?? Issue 5 — `LLM_API_KEY` Cloudflare secret was never set

After migrating from `GEMINI_API_KEY` ? `LLM_API_KEY`, the secret must be added to Cloudflare or every AI Coach request returns 500.

```bash
cd website/worker
wrangler secret put LLM_API_KEY --env production
# Paste: gsk_YOUR_GROQ_API_KEY_HERE

wrangler deploy --env production
```

---

### ?? Issue 6 — Wrong function comment label in Worker

**File:** `website/worker/src/index.js` — Line 1185

```js
// ? Current
// -- POST /api/notify-coach (sends WhatsApp to coach via Meta Cloud API) --
async function handleAiCoach(request, env) {

// ? Fix
// -- POST /api/ai-coach (proxies chat request to Groq LLM API) --
async function handleAiCoach(request, env) {
```

---

### ?? Issue 7 — Literal `\n` before `</body>` in training_tool.html

**File:** `website/tools/training_tool.html` — Line 9168

Raw `\n` escape sequence left at end of file. Cosmetic only.

```html
<!-- ? Current -->
\n</body>

<!-- ? Fix -->
</body>
```

---

## Priority Order

| Priority | # | Severity | File | Issue |
|----------|---|----------|------|-------|
| 1 | 1 | ?? Critical | `worker/src/index.js` L1218 | Broken Bearer template literal |
| 2 | 5 | ?? Medium | Cloudflare Dashboard | LLM_API_KEY secret never set |
| 3 | 2 | ?? Critical | `training_tool.html` L9116 | Relative AI Coach endpoint URL |
| 4 | 3 | ?? Medium | `training_tool.html` L9157 | Stale Gemini error message |
| 5 | 4 | ?? Medium | `access-control.js` L209, L214 | Testing bypass left active |
| 6 | 6 | ?? Low | `worker/src/index.js` L1185 | Wrong function comment |
| 7 | 7 | ?? Low | `training_tool.html` L9168 | Literal \n before </body> |

---

## Verification Checklist

- [ ] Worker deploys without errors (`wrangler deploy --env production`)
- [ ] `POST /api/ai-coach` returns streamed response (not 500)
- [ ] AI Coach FAB in training tool sends a message and gets a reply
- [ ] Access control overlay shows correctly without bypass (if Issue 4 fixed)
- [ ] `python -m pytest tests/` — all 192 pass

